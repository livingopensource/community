/* import { Session } from "$lib/server/databases/pg/users.js";

export async function load(event) {
    if (event.cookies.get("losf") == null) {
        Session.getUser(event.cookies.get("losf"))
    }
} */